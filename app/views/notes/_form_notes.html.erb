<%= form_for(note) do |f| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form_container">

  <div class="field">
    <div class="form_title">Note title</div>
    <div><%= f.text_field :noteTitle, placeholder: "Your note title here" %></div>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

</div>

<% end %>
