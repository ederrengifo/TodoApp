<%= form_for(tool) do |f| %>
  <% if tool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tool.errors.count, "error") %> prohibited this tool from being saved:</h2>

      <ul>
      <% tool.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form_container">

  <div class="field">
    <div class="form_title">Tool name</div>
    <div><%= f.text_field :tool, placeholder:"Enter the name of your tool" %></div>
    <div class="form_title">Link</div>
    <div><%= f.text_field :link, placeholder:"Paste the URL" %></div>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>

</div>

<% end %>
